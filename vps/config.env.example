# =============================================================================
# K3s Stack Configuration
# =============================================================================
# Copy this file to config.env and customize for your environment:
#   cp config.env.example config.env
#
# This file is sourced by setup scripts to configure cluster resources.
# =============================================================================

# -----------------------------------------------------------------------------
# SCM Provider Configuration (for ArgoCD auto-discovery)
# -----------------------------------------------------------------------------
# Provider: "github" or "gitlab"
SCM_PROVIDER="github"

# Organization or group name (GitHub org / GitLab group)
SCM_ORGANIZATION="my-org"

# -----------------------------------------------------------------------------
# Container Registry Configuration
# -----------------------------------------------------------------------------
# Base URL for container images (without repository name)
# Examples:
#   - GitHub Container Registry: ghcr.io/my-org
#   - GitLab Registry: registry.gitlab.com/my-group
#   - Docker Hub: docker.io/myuser
REGISTRY_BASE="ghcr.io/my-org"

# -----------------------------------------------------------------------------
# Domain Configuration
# -----------------------------------------------------------------------------
# Primary domain for the cluster (used for Gateway TLS, CoreDNS, etc.)
# Example: example.com → generates auth.example.com, alpha.example.com, etc.
DOMAIN="example.com"

# -----------------------------------------------------------------------------
# Let's Encrypt Configuration
# -----------------------------------------------------------------------------
# Email for certificate expiration notifications
LETSENCRYPT_EMAIL="admin@example.com"
